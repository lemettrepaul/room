import React from 'react';

import {Text, View} from 'react-native';
import PhoneInput from '../../components/PhoneInput';

const Connection = () => {
    return (
        <View>
            <Text>Hello, rentre ton num stp</Text>
            <PhoneInput/>
        </View>
    );
};

export default Connection;
